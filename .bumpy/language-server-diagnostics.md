---
"@nmbl-lang/language-server": minor
---

Lint findings are reported as diagnostics alongside compile errors.

Also two packaging fixes: the ESM build of `vscode-html-languageservice` is bundled correctly, and a CJS-resolvable export was added so the VS Code extension activates.
