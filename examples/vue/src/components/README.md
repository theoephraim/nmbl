# Vue Components

Custom components for testing intellisense and attributes with NMBL templates:

- **ItemCard.vue** - Card component with various props, slots, and events
- **Badge.vue** - Simple badge with color variants
- **Button.vue** - Button with size/variant options and loading state

Note: Vue slot shorthand `template(#slotName)` is now supported!