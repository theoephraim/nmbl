// Shared external links, centralized so they're a one-line change.

// VS Code Marketplace listing for the nmbl extension.
// TODO(launch): confirm the published itemName (publisher.name) before pushing
// this widely — publisher is `nmbl`; the extension name is assumed `nmbl`.
export const VSCODE_EXTENSION_URL =
  'https://marketplace.visualstudio.com/items?itemName=nmbl.nmbl';

export const GITHUB_URL = 'https://github.com/theoephraim/nmbl';

// Editor/IDE support hub — CTAs point here (not straight to a marketplace) so we
// can add more editors (Zed, JetBrains, …) without touching every link.
export const EDITOR_PAGE = '/guides/editor';
