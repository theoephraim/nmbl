// AUTO-GENERATED by src/gen-ast-types.ts — do not edit by hand.
// Typed CST for the parser output of this grammar.
//
// LIMITATION — no named fields: the grammar carries no labels on rule
// elements (e.g. `[$, '.', Ident]` has no field names), so children are
// typed by *kind* (which Node rules / leaf token types can appear), not as
// named accessors like `node.name`/`node.value`. Named-field accessors would
// require adding field labels to the grammar DSL (a future enrichment).

export interface CstPos {
  offset: number;
  end: number;
}

/** Every `tokenType` a CstLeaf in this grammar can carry. */
export type TokenType =
  | 'AtKeyword'
  | 'AttrName'
  | 'BareValue'
  | 'BlockComment'
  | 'ClassSel'
  | 'ComponentName'
  | 'DQString'
  | 'Dedent'
  | 'IdSel'
  | 'Indent'
  | 'Newline'
  | 'RawContent'
  | 'RenderedBlockComment'
  | 'RenderedComment'
  | 'SQString'
  | 'SilentComment'
  | 'TagName'
  | 'Template'
  | 'TextChunk'
  | '$keyword'
  | '$punct'
  | '$operator'
  | '$templateHead'
  | '$templateMiddle'
  | '$templateTail';

/** A terminal: one lexer token (or synthetic keyword/punct/operator leaf). */
export interface CstLeaf extends CstPos {
  tokenType: TokenType;
}

/** Synthetic node the parser builds for an interpolated template literal. */
export interface $templateNode extends CstPos {
  rule: '$template';
  children: CstChild[];
}

/** `ExprBody` node. Children (flattened, in source order) are drawn from: */
export interface ExprBodyNode extends CstPos {
  rule: 'ExprBody';
  children: Array<
    | $templateNode
    | (CstLeaf & { tokenType: '$punct' })
    | (CstLeaf & { tokenType: '$templateHead' })
    | (CstLeaf & { tokenType: 'AttrName' })
    | (CstLeaf & { tokenType: 'BareValue' })
    | (CstLeaf & { tokenType: 'DQString' })
    | (CstLeaf & { tokenType: 'SQString' })
    | (CstLeaf & { tokenType: 'Template' })
    | ExprBodyNode
  >;
}

/** `AttrValue` node. Children (flattened, in source order) are drawn from: */
export interface AttrValueNode extends CstPos {
  rule: 'AttrValue';
  children: Array<
    | $templateNode
    | (CstLeaf & { tokenType: '$punct' })
    | (CstLeaf & { tokenType: '$templateHead' })
    | (CstLeaf & { tokenType: 'AttrName' })
    | (CstLeaf & { tokenType: 'BareValue' })
    | (CstLeaf & { tokenType: 'DQString' })
    | (CstLeaf & { tokenType: 'SQString' })
    | (CstLeaf & { tokenType: 'Template' })
    | ExprBodyNode
  >;
}

/** `Attr` node. Children (flattened, in source order) are drawn from: */
export interface AttrNode extends CstPos {
  rule: 'Attr';
  children: Array<
    | (CstLeaf & { tokenType: '$punct' })
    | (CstLeaf & { tokenType: 'AttrName' })
    | AttrValueNode
  >;
}

/** `AttrList` node. Children (flattened, in source order) are drawn from: */
export interface AttrListNode extends CstPos {
  rule: 'AttrList';
  children: Array<
    | (CstLeaf & { tokenType: '$punct' })
    | AttrNode
  >;
}

/** `TextSoup` node. Children (flattened, in source order) are drawn from: */
export interface TextSoupNode extends CstPos {
  rule: 'TextSoup';
  children: Array<
    | $templateNode
    | (CstLeaf & { tokenType: '$punct' })
    | (CstLeaf & { tokenType: '$templateHead' })
    | (CstLeaf & { tokenType: 'AtKeyword' })
    | (CstLeaf & { tokenType: 'AttrName' })
    | (CstLeaf & { tokenType: 'BareValue' })
    | (CstLeaf & { tokenType: 'ClassSel' })
    | (CstLeaf & { tokenType: 'ComponentName' })
    | (CstLeaf & { tokenType: 'DQString' })
    | (CstLeaf & { tokenType: 'IdSel' })
    | (CstLeaf & { tokenType: 'SQString' })
    | (CstLeaf & { tokenType: 'TagName' })
    | (CstLeaf & { tokenType: 'Template' })
    | (CstLeaf & { tokenType: 'TextChunk' })
  >;
}

/** `TagHead` node. Children (flattened, in source order) are drawn from: */
export interface TagHeadNode extends CstPos {
  rule: 'TagHead';
  children: Array<
    | (CstLeaf & { tokenType: 'ClassSel' })
    | (CstLeaf & { tokenType: 'ComponentName' })
    | (CstLeaf & { tokenType: 'IdSel' })
    | (CstLeaf & { tokenType: 'TagName' })
  >;
}

/** `Element` node. Children (flattened, in source order) are drawn from: */
export interface ElementNode extends CstPos {
  rule: 'Element';
  children: Array<
    | (CstLeaf & { tokenType: '$punct' })
    | (CstLeaf & { tokenType: 'Dedent' })
    | (CstLeaf & { tokenType: 'Indent' })
    | (CstLeaf & { tokenType: 'RawContent' })
    | AttrListNode
    | ElementNode
    | LinesNode
    | TagHeadNode
    | TextSoupNode
  >;
}

/** `PipeText` node. Children (flattened, in source order) are drawn from: */
export interface PipeTextNode extends CstPos {
  rule: 'PipeText';
  children: Array<
    | (CstLeaf & { tokenType: '$punct' })
    | TextSoupNode
  >;
}

/** `AtBlock` node. Children (flattened, in source order) are drawn from: */
export interface AtBlockNode extends CstPos {
  rule: 'AtBlock';
  children: Array<
    | (CstLeaf & { tokenType: '$punct' })
    | (CstLeaf & { tokenType: 'AtKeyword' })
    | (CstLeaf & { tokenType: 'Dedent' })
    | (CstLeaf & { tokenType: 'Indent' })
    | ExprBodyNode
    | LinesNode
  >;
}

/** `Line` node. Children (flattened, in source order) are drawn from: */
export interface LineNode extends CstPos {
  rule: 'Line';
  children: Array<
    | (CstLeaf & { tokenType: 'RawContent' })
    | (CstLeaf & { tokenType: 'RenderedBlockComment' })
    | (CstLeaf & { tokenType: 'RenderedComment' })
    | AtBlockNode
    | ElementNode
    | PipeTextNode
    | TextSoupNode
  >;
}

/** `Lines` node. Children (flattened, in source order) are drawn from: */
export interface LinesNode extends CstPos {
  rule: 'Lines';
  children: Array<
    | (CstLeaf & { tokenType: 'Newline' })
    | LineNode
  >;
}

/** `Document` node. Children (flattened, in source order) are drawn from: */
export interface DocumentNode extends CstPos {
  rule: 'Document';
  children: Array<
    | LinesNode
  >;
}

/** Discriminated union of every node kind. Switch on `node.rule` for exhaustiveness. */
export type CstNode =
  | $templateNode
  | ExprBodyNode
  | AttrValueNode
  | AttrNode
  | AttrListNode
  | TextSoupNode
  | TagHeadNode
  | ElementNode
  | PipeTextNode
  | AtBlockNode
  | LineNode
  | LinesNode
  | DocumentNode;

/** Every `rule` discriminant value (the keys of the CstNode union). */
export type RuleName =
  | '$template'
  | 'ExprBody'
  | 'AttrValue'
  | 'Attr'
  | 'AttrList'
  | 'TextSoup'
  | 'TagHead'
  | 'Element'
  | 'PipeText'
  | 'AtBlock'
  | 'Line'
  | 'Lines'
  | 'Document';

/** Any CST element: a node or a leaf. */
export type CstChild = CstNode | CstLeaf;

/** Narrow the CstNode union to the node for a given rule name. */
export type NodeOf<R extends RuleName> = Extract<CstNode, { rule: R }>;
